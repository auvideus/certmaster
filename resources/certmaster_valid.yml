---
server:
  email: someguy@example.com
  poll_interval: "5m"
  dry_run: true
client:
  poll_interval: "5m"
  priv_key: /root/.ssh/id_rsa_certmaster
  path: /etc/letsencrypt
  host: localhost
digital_ocean:
  token: token1
domains:
- name: example.com
  subdomains:
  - app.example.com
- name: example2.com
